# Github-Issue-Tracker

<h1> Live Link : <a href="">Click here</a> </h1>

<ul>
    <li>1️⃣ What is the difference between var, let, and const?</li>
    <li>2️⃣ What is the spread operator (...)?</li>
    <li>3️⃣ What is the difference between map(), filter(), and forEach()?</li>
    <li>4️⃣ What is an arrow function?</li>
    <li>5️⃣ What are template literals?</li>
</ul>

<h3>Answers</h2>
<ol>
    <li>Let and Const are hoisted whereas var isn't. "Let" lets you change the value after initializaiton but const is unchangeable </li>
    <li>spread operator is used to spread or unpack the content of an array or object </li>
    <li>
     Map - maps array elements, maybe with some logic and returns a new array
     Filter - filter items for an array based on conditions and returns a new array.
     forEach - loops through array elements.
    </li>
    <li>
        Array Function- Shortand way of writing a js function 
         (params)=> {}  instead of function name (param){}
    </li>
    <li>Template Literals are a way to write strings with special functionality of directly using variables inside the string: 
    template literals uses `` (backtick) and inside any string can be written alongside accessing the variable using ${variableName} syntax</li>
</ol>
